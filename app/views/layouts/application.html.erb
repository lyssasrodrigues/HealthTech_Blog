<!DOCTYPE html>
<html>
  <head>
    <title>HealthInsights Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
      body {
        background-color: #f8fafc;
      }
      .hero {
        background: linear-gradient(135deg, #0066cc 0%, #00b4d8 100%);
        color: white;
        padding: 5rem 1rem;
        text-align: center;
        border-radius: 0 0 2rem 2rem;
      }
      .hero h1 {
        font-weight: 700;
      }
      .hero p {
        font-size: 1.25rem;
      }
      .article-card:hover {
        transform: translateY(-5px);
        transition: 0.3s;
      }
    </style>
  </head>

  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
      <div class="container">
        <a class="navbar-brand fw-bold" href="/">HealthTech</a>
        <div>
          <%= link_to "Artigos", articles_path, class: "nav-link d-inline text-white me-3" %>
          <%= link_to "Novo Artigo", new_article_path, class: "btn btn-light btn-sm" %>
        </div>
      </div>
    </nav>

    <%= yield %>

    <footer class="text-center text-muted py-4 border-top mt-5 bg-white">
      © <%= Time.current.year %> <strong>HealthInsights</strong> — Inovação e Tecnologia em Saúde
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
