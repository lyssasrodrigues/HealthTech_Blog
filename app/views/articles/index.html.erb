<!-- Hero Section -->
<section class="hero mb-5">
  <div class="container">
    <h1>HealthTech Blog</h1>
    <p>Explorando como a tecnologia e a inteligência artificial estão transformando a saúde.</p>
  </div>
</section>

<!-- Artigo em Destaque -->
<div class="container mb-5">
  <div class="card shadow-lg border-0 p-4 bg-white">
    <div class="row align-items-center">
      <div class="col-lg-7">
        <h2 class="text-primary fw-bold mb-3">Como a IA pode revolucionar o campo da saúde</h2>
        <p class="lead text-muted">
          A inteligência artificial tem potencial para transformar o setor da saúde, desde diagnósticos mais rápidos
          até a personalização do tratamento. Nesse artigo, compartilho minha visão sobre como a IA pode impactar a
          vida de pacientes e profissionais, e por que acredito tanto nessa área.
        </p>
        <%= link_to "Ler artigo completo", article_path(@articles.first), class: "btn btn-primary mt-3" %>
      </div>
      <div class="col-lg-5 text-center">
        <img src="https://cdn-icons-png.flaticon.com/512/3448/3448590.png" alt="IA e Saúde" class="img-fluid" style="max-height: 220px;">
      </div>
    </div>
  </div>
</div>

<!-- Lista de Artigos -->
<div class="container">
  <h3 class="text-center text-secondary mb-4">Outros Artigos Recentes</h3>
  <div class="row g-4">
    <% @articles.each do |article| %>
      <div class="col-12 col-md-6 col-lg-4">
        <div class="card article-card border-0 shadow-sm h-100">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title text-primary"><%= link_to article.title, article, class: "stretched-link text-decoration-none" %></h5>
            <p class="small text-muted">por <%= article.user.name %></p>
            <p class="text-muted small mb-2"><%= article.category || "Geral" %> • <%= article.created_at.strftime("%d/%m/%Y") %></p>
            <p class="card-text text-muted"><%= truncate(strip_tags(article.summary || article.body), length: 120) %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
